{
  "name": "@parmana/connector-paytm",
  "version": "0.1.0",
  "private": true,
  "description": "Paytm refund connector for Parmana, the authorization layer for AI execution: forwards a Parmana-authorized paytm:refund capability to the trusted, out-of-process Paytm connector service (parmana-paytm-agent) over HTTPS. Never calls Paytm's own API directly.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@parmana/connector-sdk": "0.1.0",
    "@parmana/crypto": "0.1.0",
    "@parmana/shared": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "SEE LICENSE IN LICENSE"
}
