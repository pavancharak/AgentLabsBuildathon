---
title: "Create (execute) a Business Transaction"
description: "Identical to `POST /execute` in every way except the success status: this endpoint returns `201` where `POST /execute` returns `200`."
openapi: "/openapi.bundled.yaml POST /transactions"
---
