---
title: "Capture an email address before unlocking the handbook PDF download"
description: "Backs a programmatic caller that wants a `downloadUrl` back in a JSON response body instead of a redirect -- see GET /handbook/download-leads above for the browser-form variant, which is what the actual docs site page uses."
openapi: "/openapi.bundled.yaml POST /handbook/download-leads"
---
