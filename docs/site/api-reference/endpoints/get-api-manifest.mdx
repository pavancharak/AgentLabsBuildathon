---
title: "Machine-readable SDK and discovery manifest"
description: "A single machine-readable index for coding agents and tooling: the OpenAPI spec locations, the rendered documentation views, the authentication scheme, and both official SDKs (TypeScript and Python) with their real, current package name,..."
openapi: "/openapi.bundled.yaml GET /api-manifest.json"
---
