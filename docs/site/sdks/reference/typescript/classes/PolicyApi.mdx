---
title: "Class: PolicyApi"
---

Defined in: [typescript/src/client/PolicyApi.ts:49](https://github.com/pavancharak/AgentLabsBuildathon/blob/main/typescript/src/client/PolicyApi.ts#L49)

Policy API.

## Constructors

### Constructor

> **new PolicyApi**(`transport`): `PolicyApi`

Defined in: [typescript/src/client/PolicyApi.ts:50](https://github.com/pavancharak/AgentLabsBuildathon/blob/main/typescript/src/client/PolicyApi.ts#L50)

#### Parameters

##### transport

[`Transport`](/sdks/reference/typescript/interfaces/Transport)

#### Returns

`PolicyApi`

## Methods

### validate()

> **validate**(`policyId`, `policyVersion`): `Promise`\<[`PolicyValidationResult`](/sdks/reference/typescript/interfaces/PolicyValidationResult)\>

Defined in: [typescript/src/client/PolicyApi.ts:69](https://github.com/pavancharak/AgentLabsBuildathon/blob/main/typescript/src/client/PolicyApi.ts#L69)

Confirms that a policy (name + version) is loadable by the Runtime.

POST /policies/validate does not accept or check a policy
document body — it only checks that
policyRepository.load(policyId, policyVersion) succeeds
(packages/api/src/routes/policies.ts).

POST /policies/validate does not use the shared `{error, code?}`
envelope for 400 or 404: both are `{valid, errors}`, its own shape,
the caller's answer, not an SDK-level failure — so those two
statuses are exempted from the default throw-on-error behavior. 401
is not exempted: it is generated by caller-auth middleware before
this route's own handler ever runs, and does use the shared
envelope, identically to every other route. See
/api-reference/error-handling.

#### Parameters

##### policyId

`string`

##### policyVersion

`string`

#### Returns

`Promise`\<[`PolicyValidationResult`](/sdks/reference/typescript/interfaces/PolicyValidationResult)\>

---

### proposeChange()

> **proposeChange**(`name`, `version`, `input`): `Promise`\<[`ProposedPolicyChange`](/sdks/reference/typescript/interfaces/ProposedPolicyChange)\>

Defined in: [typescript/src/client/PolicyApi.ts:96](https://github.com/pavancharak/AgentLabsBuildathon/blob/main/typescript/src/client/PolicyApi.ts#L96)

Proposes a policy change. Maps to POST
/policies/{name}/{version}/pending-changes. The API key must belong to
a verified human (credential holder type USER). `proposedContent.policyId`
must equal `name`. Only one open proposal per name and version is
allowed; a second one fails with ConflictError.

#### Parameters

##### name

`string`

##### version

`string`

##### input

[`ProposePolicyChangeInput`](/sdks/reference/typescript/interfaces/ProposePolicyChangeInput)

#### Returns

`Promise`\<[`ProposedPolicyChange`](/sdks/reference/typescript/interfaces/ProposedPolicyChange)\>

---

### listChanges()

> **listChanges**(`status?`): `Promise`\<[`PolicyChangeForReview`](/sdks/reference/typescript/interfaces/PolicyChangeForReview)[]\>

Defined in: [typescript/src/client/PolicyApi.ts:117](https://github.com/pavancharak/AgentLabsBuildathon/blob/main/typescript/src/client/PolicyApi.ts#L117)

Lists policy changes for review, with the content in effect now next to
the proposed content. Maps to GET /policies/pending-changes. The API key
must belong to a verified human.

#### Parameters

##### status?

[`PendingPolicyChangeStatus`](/sdks/reference/typescript/type-aliases/PendingPolicyChangeStatus)

Only changes in this state. Omit for all.

#### Returns

`Promise`\<[`PolicyChangeForReview`](/sdks/reference/typescript/interfaces/PolicyChangeForReview)[]\>

---

### approveChange()

> **approveChange**(`pendingPolicyChangeId`, `stepUpAuthorization`): `Promise`\<[`PendingPolicyChange`](/sdks/reference/typescript/interfaces/PendingPolicyChange)\>

Defined in: [typescript/src/client/PolicyApi.ts:140](https://github.com/pavancharak/AgentLabsBuildathon/blob/main/typescript/src/client/PolicyApi.ts#L140)

Approves a policy change, which then takes effect. Maps to POST
/policies/pending-changes/{id}/approve. The API key must belong to a
verified human with a registered step up key, who is not the proposer.
Make `stepUpAuthorization` with `signPolicyChangeStepUp()` and action
"approve"; it is valid once, for 120 seconds by default.

#### Parameters

##### pendingPolicyChangeId

`string`

##### stepUpAuthorization

[`PolicyChangeStepUpAuthorization`](/sdks/reference/typescript/interfaces/PolicyChangeStepUpAuthorization)

#### Returns

`Promise`\<[`PendingPolicyChange`](/sdks/reference/typescript/interfaces/PendingPolicyChange)\>

---

### rejectChange()

> **rejectChange**(`pendingPolicyChangeId`, `rejectionReason`, `stepUpAuthorization`): `Promise`\<[`PendingPolicyChange`](/sdks/reference/typescript/interfaces/PendingPolicyChange)\>

Defined in: [typescript/src/client/PolicyApi.ts:158](https://github.com/pavancharak/AgentLabsBuildathon/blob/main/typescript/src/client/PolicyApi.ts#L158)

Rejects a policy change. Maps to POST
/policies/pending-changes/{id}/reject. Same caller rules as
approveChange(); sign with action "reject". A reason is required.

#### Parameters

##### pendingPolicyChangeId

`string`

##### rejectionReason

`string`

##### stepUpAuthorization

[`PolicyChangeStepUpAuthorization`](/sdks/reference/typescript/interfaces/PolicyChangeStepUpAuthorization)

#### Returns

`Promise`\<[`PendingPolicyChange`](/sdks/reference/typescript/interfaces/PendingPolicyChange)\>
